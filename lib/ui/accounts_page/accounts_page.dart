
import 'package:flutter/material.dart';

class AccountsPage extends StatelessWidget {
  const AccountsPage({Key? key}) : super(key: key);

  static String get routeName => '/accounts';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}